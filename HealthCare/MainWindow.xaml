<Window x:Class="HealthCare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TitleName}"  Height="768" Width="1024" 
        Icon="{Binding Logo}" 
        WindowStartupLocation="CenterScreen" WindowState="Maximized" Closed="Window_Closed"
        >
    <Grid>
        <Frame x:Name="PageFrame" NavigationUIVisibility="Hidden"></Frame>
        <!--<Grid Height="{Binding ElementName=PageFrame, Path=Height}" Width="{Binding ElementName=PageFrame, Path=Width}" Background="#7d000000" Visibility="{Binding MaskShow, Converter={StaticResource BooleanToVisibilityParamConverter}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" >
        </Grid>-->
    </Grid>
</Window>
